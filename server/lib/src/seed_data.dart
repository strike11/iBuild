/// Seed catalogue (JSON maps). Wire shape matches `b2c/lib/models` — keep in sync.
library;

/// Empty catalogue — projects are added via B2B admin or database import.
List<Map<String, dynamic>> buildProjectsSeed() => [];
