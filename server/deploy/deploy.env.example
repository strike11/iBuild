# Written by CI or first-deploy.sh — image tag only, no application secrets.
# Lives at /opt/ibuild/deploy/.env on the server.
#
# CI example:
#   IBUILD_IMAGE=ghcr.io/strike11/ibuild/api:sha-abc123def456
#
# Local first deploy:
#   IBUILD_IMAGE=ibuild-api:local

IBUILD_IMAGE=
