// [PressableScale] now lives in the shared `ibuild_core` package. Re-exported
// here so existing `import '../widgets/pressable_scale.dart'` call sites keep
// working unchanged.
export 'package:ibuild_core/ibuild_core.dart' show PressableScale;
